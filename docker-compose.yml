https-portal:
  image: steveltn/https-portal:1
  ports:
    - "80:80"
    - "443:443"
  links:
    - nginx
  restart: always
  environment:
    DOMAINS: "kotaroooo0.com"
    STAGE: "production"
    FORCE_RENEW: "true"

nginx:
  build: "."
